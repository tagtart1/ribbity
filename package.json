{
  "name": "ribbity",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "deploy:frontend": "npm --prefix frontend run build && cd frontend && npx firebase-tools deploy --only hosting"
  }
}
